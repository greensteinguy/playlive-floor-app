import Placeholder from '../../shell/Placeholder'

export default function Tournaments() {
  return (
    <Placeholder
      title="Tournaments"
      phase="2"
      task="2.2"
      description="List view of all tournaments — running, scheduled, finished. Filter and sort; click through to a tournament's detail page (clock, tables, payouts, and the 'Register player' action). This is the entry point for cashier-led registration: pick the tournament first, then register a player into it."
    />
  )
}
